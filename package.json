{
  "name": "gtaw-image-manager-web",
  "version": "0.2.0",
  "private": false,
  "description": "Landing page for GTAW Image Manager.",
  "license": "MIT",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  },
  "engines": {
    "node": "24.x"
  }
}
